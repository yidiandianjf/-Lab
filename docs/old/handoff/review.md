# 代码审查与问题汇总

> 日期: 2026-03-22  
> 审查范围: GameEngine, StateEvolution, NPC系统, IO系统, 叙事系统  
> 审查方式: 代码走查 + 运行时测试

---

## 目录

0. [运行期已更改问题](#0-本次会话问题记录运行期)
1. [已确认的问题](#1-已确认的问题)
2. [设计决策澄清](#2-设计决策澄清)
3. [改进建议](#3-改进建议)
4. [待修复项](#4-待修复项)
5. [根本问题分析（跨问题）](#5-根本问题分析跨问题)
6. [改进计划](#6-改进计划)

---

## 0. 本次会话问题记录（运行期）

> 记录范围: 本轮联调中真实出现并复现的问题
> 记录目标: 便于后续回归、排障和提示词约束优化

### 0.1 CLI 启动失败: 缩进错误（已修复）

问题现象:
- 启动时报错 `IndentationError: unindent does not match any outer indentation level`
- 定位到 `src/cli/game_cli.py` 中角色/物品展示函数附近

根因:
- 局部代码调整后出现缩进层级不一致
- 辅助方法 `_summarize_text` 被误缩进到其他方法体内部

修复:
- 统一 `if char` 与 `if item` 分支缩进
- 将 `_summarize_text` 恢复到类方法层级

验证:
- 语法检查通过
- `src.main` 可正常导入

### 0.2 控制台编码崩溃（已修复）

问题现象:
- Windows 终端出现 `UnicodeEncodeError: 'gbk' codec can't encode character`
- 触发点包括项目符号与进度条字符显示

根因:
- CLI 输出包含部分非 ASCII 字符（如圆点符号、块状进度条）
- 在 GBK 控制台下无法稳定编码输出

修复:
- 列表前缀改为 `*`
- 进度条字符改为 `#` 和 `-`

验证:
- 状态渲染流程可完整执行，不再因编码中断

### 0.3 unhashable type: list（鉴定结果显示路径）（已修复）

问题现象:
- 游戏过程中出现 `unhashable type: 'list'`
- 发生在鉴定结果渲染及状态显示路径

根因:
- `check_result.result` 在异常输入下可能不是预期字符串/枚举，存在列表等形态
- 旧渲染逻辑直接用于字典键查询，触发不可哈希异常

修复:
- 为 `print_check_result` 增加类型归一化（dict/list/enum/string）
- 增加结果别名映射，非预期值回退到可显示文本

验证:
- 手工构造异常形态输入（含 list、dict）可正常渲染

### 0.4 unhashable type: list（地图实体列表污染路径）（已修复）

问题现象:
- 堆栈定位在 `game_state.items.get(item_id)`，其中 `item_id` 实际为 list
- CLI 显示层反复报错并刷屏

根因:
- 对列表字段执行 ADD 时，若 LLM 返回 list 值，旧逻辑使用 append 导致嵌套列表
- `entities.items` 与 `entities.characters` 被污染后，在遍历阶段把 list 当作 id 使用

修复:
- 引擎与 IO 层对 ADD(list 值) 改为 extend
- 对 `entities.items` / `entities.characters` 增加扁平化与去重清洗
- 引擎上下文构建和 CLI 展示统一改为扁平化遍历

验证:
- 新增回归测试 `test_add_entities_items_with_list_value_is_flattened` 并通过
- `tests.test_regression_flow` 全量通过

### 0.5 CLI 异常后循环刷屏（已修复）

问题现象:
- 出现异常后仍持续进入下一轮循环，造成同一错误反复刷屏

根因:
- 主循环异常分支只打印错误，不做熔断或退出控制

修复:
- 增加连续错误计数
- 达到阈值后自动退出循环，避免重复刷屏

验证:
- 异常场景下可停止刷屏并退出循环

### 0.6 运行中观测到的附带风险（待跟进）

现象:
- 日志出现 Pydantic 警告: 某字段期望字符串但收到列表

风险判断:
- 说明上游输出仍可能存在类型漂移
- 虽已在引擎与显示层做防御性处理，但建议继续收紧 LLM 输出约束与校验

建议跟进:
- 在状态变更校验阶段补充更严格的字段类型断言
- 在 Prompt 中明确 `entities.items` / `entities.characters` 的 value 形态要求

### 0.7 一致性保障体系（鉴定锚点 + 叙事广播）

需求原文要点:
- 避免出现“NPC叙事判定玩家赢/输”与“玩家侧叙事判定相反”的双真相冲突
- 以鉴定系统结果作为事实锚点（truth anchor）
- 以合并叙事结果作为唯一广播源（single source of truth for display）

已实施改造:
- 在回合处理中构建 `player_resolution_anchor`，记录玩家检定关键事实（例如行动成功与否、检定结果）
- 将锚点透传给 NPC 导演决策上下文，要求导演阶段不得改写玩家检定结论
- 将锚点透传给 NPC 推演/状态演化上下文，约束后续叙事生成不可与检定冲突
- 将锚点作为 `truth_anchor` 透传给叙事合并器，合并阶段优先服从锚点事实
- 将回合展示统一对齐到合并叙事，避免同回合同时展示多份互相矛盾文本

提示词层约束（已补充）:
- NPC 导演提示词新增“锚点优先，不得改写玩家检定胜负”
- 状态演化提示词新增“检定锚点强约束”
- 叙事合并提示词新增“truth_anchor 优先级最高”

当前一致性规则:
- 规则1: 鉴定系统输出为事实锚点，不可被导演/推演/合并链路改写
- 规则2: 同回合对外展示以合并叙事为唯一广播文本
- 规则3: 若局部叙事与锚点冲突，必须以锚点修正并在合并阶段消歧

验证与回归:
- 已补回归用例，确认玩家锚点会传入 NPCDirector 上下文
- 全量回归通过（含新增一致性相关用例）

备注:
- 该项是本轮最核心的架构保障：把“检定事实”与“叙事表达”解耦，并通过锚点/广播机制重新收敛为单一真相。

---

## 1. 已确认的问题

### 1.1 回合数不增加 (P0)

**问题描述**: 玩家输入行动后，回合数没有正常增加，始终停留在第1回合。

**根本原因**: `StateEvolution` 返回的 `resolved` 字段默认为 `false`，导致 `GameEngine._turn_end()` 方法不增加回合数。

**代码位置**:
- `src/engine/game_engine.py` 第376-379行
- `src/agent/state_evolution.py` 第687-733行

**当前逻辑**:
```python
def _turn_end(self, resolved: bool = True):
    if resolved:
        self.game_state.turn_count += 1  # 只有resolved=True时才增加
    else:
        logger.debug("回合未解决，保持当前行动者")  # resolved=False时不增加
```

**影响**: 影响游戏进程跟踪、回合制机制、叙事上下文的时间线。

**修复建议**: 
1. 修改提示词，明确大多数行动应返回 `resolved=true`
2. 或修改 `_turn_end` 方法，强制每轮都增加回合数

---

### 1.2 纯对话无法触发NPC响应 (P0)

**问题描述**: 玩家输入"和守卫说话"等对话类指令时，NPC不会做出回应。

**根本原因**: 当 `is_dialogue=true` 时，`GameEngine` 直接返回 DM Agent 的回复，不执行后续的 NPC 响应流程。

**代码位置**:
- `src/engine/game_engine.py` 第376-379行

**当前逻辑**:
```python
if dm_output.is_dialogue:
    result["response"] = dm_output.response_to_player
    self._record_dm_dialogue(natural_input, dm_output.response_to_player)
    return result  # <-- 直接返回，不执行NPC响应
```

**影响**: 玩家无法与NPC进行基础对话，影响游戏交互体验。

**修复建议**:
1. 修改 `GameEngine` 逻辑：即使 `is_dialogue=true`，如果有 `npc_response_needed=true`，仍需执行NPC响应流程
2. 修改 DM Agent 提示词：明确纯对话时仍需设置 `npc_response_needed=true`

**修复后的逻辑**:
```python
if dm_output.is_dialogue:
    if dm_output.npc_response_needed and dm_output.npc_actor_id:
        # 继续执行NPC响应流程，不返回
        pass
    else:
        # 没有NPC响应，直接返回
        result["response"] = dm_output.response_to_player
        return result
```

---



---



---

### 1.4 IO一致性检查缺失 (P1)

**问题描述**: 系统缺少物品位置与持有者inventory的双向一致性检查。

**具体表现**:
1. 钥匙的 `location = "char-player-01"`，但玩家 `inventory` 中没有该钥匙ID → **不报错**
2. 同一个钥匙ID同时出现在玩家背包和地图中 → **不报错**
3. 玩家 `inventory` 中有钥匙，但钥匙的 `location` 指向其他地方 → **不报错**

**根本原因**: 
- 文档中设计的 `ConsistencyChecker` 类未实现
- `IOSystem` 只验证单个变更的合法性，不验证多个变更之间的一致性

**代码位置**:
- `src/data/io_system.py` 第425-441行 (`apply_changes`)
- `src/agent/state_evolution.py` 第805-874行 (`validate_changes`)

**影响**: 数据不一致，可能导致游戏逻辑错误（如物品消失或重复）。

**修复建议**:
1. 实现 `ConsistencyChecker` 类，检查：
   - 物品 `location` 与持有者 `inventory` 的双向一致性
   - 同一物品是否同时存在于多个位置
2. 在 `StateEvolution._call_evolution()` 中添加一致性校验
3. 在 `GameEngine` 中定期运行一致性检查

**示例修复代码**:
```python

```

---

### 1.5 物品移动无自动同步 (P2)

**问题描述**: 物品移动需要手动更新3个字段，系统不会自动同步。

**当前行为**:
- LLM只输出 `{"id": "item-key-01", "field": "location", "value": "char-player-01"}`
- 系统**不会自动**从原持有者inventory中移除
- 系统**不会自动**添加到新持有者的inventory中

**代码位置**:
- `src/data/io_system.py` 第425-441行
- `src/agent/input_system.py` 第323-387行 (pickup命令手动处理3个变更)

**影响**: 
- LLM容易产生不一致的变更
- 增加Agent开发复杂度
- 数据不一致风险

**修复建议**:
1. **方案A - 自动同步机制**: 在 `IOSystem` 中检测物品location变更，自动处理inventory同步
2. **方案B - 简化LLM输出**: 允许LLM输出简化的转移指令，系统自动展开为多个变更
3. **方案C - 级联变更**: 在 `StateEvolution` 中根据单个变更自动生成关联变更

**示例修复代码（方案A）**:

```

---

### 1.6 读档未恢复世界级配置，导致规则漂移 (P1)

**问题描述**: 存档只保存 `GameState` 与叙事上下文，未保存并恢复 `world_name`、`end_condition`、`npc_response_mode`、LLM开关等世界级配置。

**根本原因**:
- `save_game()` 未写入世界级配置字段
- `load_game()` 恢复后未调用 `apply_world_settings()`

**代码位置**:
- `src/engine/game_engine.py` 第209-239行 (`save_game`)
- `src/engine/game_engine.py` 第170-207行 (`load_game`)

**影响**:
- 跨世界或非默认配置读档后，行为可能回退到默认值
- 结局判定规则、NPC响应策略、叙事合并策略可能与存档时不一致

**修复建议**:
1. 存档时写入 world-level metadata（world_name / end_condition / npc_mode / narrative_window / llm switches）
2. 读档后先恢复 metadata，再调用 `apply_world_settings()` 对齐运行态
3. 增加“跨世界存档-读档配置一致性测试”

---

### 1.7 变更失败仅记录日志，未阻断流程导致叙事与状态分叉 (P1)

**问题描述**: 当 `io.apply_state_change()` 返回非0错误码时，系统仅打印 warning 并继续流程，不会反馈给上游或中止当前回合。

**根本原因**:
- `_apply_changes()` 只做逐条日志记录，不聚合失败、不回滚、不改写 `result["success"]`
- `StateEvolution.validate_changes()` 对字段路径仅做“非空检查”，无法提前发现大量无效字段

**代码位置**:
- `src/engine/game_engine.py` 第773-790行 (`_apply_changes`)
- `src/agent/state_evolution.py` 第849-853行 (`validate_changes` 字段路径检查)

**影响**:
- 玩家看到叙事“已发生”，但状态未落地，形成隐式数据分叉
- 多条变更部分成功时，可能引入难复现的中间态

**修复建议**:
1. `_apply_changes()` 返回失败列表并由 `process_input()` 统一处理（降级/重试/中止）
2. 对同一轮变更采用“全成功才提交”的事务式策略（至少在内存态实现原子性）
3. 在 `validate_changes()` 增加字段路径可达性检查（含嵌套字段）

---

## 2. 设计决策澄清

### 2.1 叙事系统架构

**澄清**: 叙事系统分为三个层次，各自有不同的处理方式：

| 层次 | 功能 | 是否使用LLM | 说明 |
|------|------|-------------|------|
| **叙事生成** | StateEvolution生成玩家/NPC行动结果叙事 | ✅ 是 | 每个行动者独立生成叙事片段 |
| **叙事合并** | 将多个独立叙事合并为完整连贯的全局叙事 | ✅ **是** | 解决冲突、统一视角 |
| **上下文压缩** | 历史事件超出窗口时的压缩处理 | ❌ 否 | 使用滚动窗口机制，简单截断 |

**关键原则**: 合并后的全局叙事必须通过 `NarrativeContext.get_context_for_llm()` 提供给所有LLM模块作为上下文提示词。

---

### 2.2 NPC决策分工

**澄清**: NPCDirector 和 StateEvolution 的分工：

| 模块 | 职责 | 输出 |
|------|------|------|
| **NPCDirector** | 决定NPC行动意图 | `NPCActionForm`（行动类型、目标、意图描述） |
| **StateEvolution** | 执行NPC行动，生成具体叙事 | `StateEvolutionOutput`（叙事、状态变更） |

**StateEvolution中的NPC推演指南需要保留**，但角色从"决定NPC做什么"变为"根据NPCDirector的决定，生成NPC怎么做"。

---

### 2.3 玩家-NPC对话支持

**澄清**: 系统已支持玩家与NPC对话，但存在实现缺陷：

- ✅ `is_dialogue` 字段可识别纯对话
- ✅ `NPCActionType.TALK` 已定义
- ✅ `NPCDirector` 支持对话决策
- ❌ **缺陷**: `is_dialogue=true` 时直接返回，不触发NPC响应

---

## 3. 改进建议

### 3.1 高优先级

1. **修复回合数不增加问题**
   - 修改 `state_evolution_prompt.md`，明确 `resolved` 字段使用规则
   - 或修改 `GameEngine._turn_end()` 强制增加回合数

2. **修复纯对话NPC响应问题**
   - 修改 `GameEngine` 逻辑，支持 `is_dialogue=true` 时的NPC响应
   - 修改 `system_prompt.md`，明确纯对话时仍需设置 `npc_response_needed=true`


### 3.2 中优先级

4. **实现IO一致性检查器**
   - 创建 `ConsistencyChecker` 类
   - 检查物品location与inventory的双向一致性
   - 在关键流程中添加一致性校验

5. **实现物品移动自动同步**
   - 在 `IOSystem` 中检测物品location变更
   - 自动处理inventory的添加/移除

6. **修复读档配置漂移**
   - 存档写入 world-level metadata
   - 读档后恢复 metadata 并调用 `apply_world_settings()`

7. **收敛变更失败处理策略**
   - `_apply_changes()` 聚合失败并向主流程返回
   - 为单回合变更增加事务/回滚或失败降级策略
   - 补齐字段路径可达性校验

### 3.3 低优先级

8. **优化叙事合并器**
   - 实现 `NarrativeMerger` 类
   - 创建 `narrative_merger_prompt.md`
   - 集成到 `GameEngine` 流程中

9. **增强社交互动支持**
   - 在规则系统中添加社交技能定义（说服、话术、恐吓等）
   - 细化DMAgent的社交意图识别

---

## 4. 待修复项

### 4.1 代码修复清单

| 优先级 | 文件 | 修改内容 | 预计工作量 |
|--------|------|----------|------------|
| P0 | `src/engine/game_engine.py` | 修复纯对话时的NPC响应逻辑 | 2小时 |
| P0 | `src/agent/prompt/system_prompt.md` | 更新is_dialogue说明，添加NPC响应指导 | 1小时 |
| P0 | `src/agent/prompt/state_evolution_prompt.md` | 明确resolved字段使用规则 | 1小时 |
| P1 | `src/engine/game_engine.py` | 在主流程接入NPC前置回合消费逻辑 | 2小时 |
| P1 | `src/agent/state_evolution.py` | 添加数值范围验证 | 1小时 |
| P1 | `src/agent/state_evolution.py` | 添加字段路径可达性校验 | 1小时 |
| P1 | `src/engine/game_engine.py` | 变更失败聚合与失败策略（阻断/降级/回滚） | 2小时 |
| P1 | `src/engine/game_engine.py` | 存档写入并读档恢复world-level metadata | 2小时 |
| P1 | `src/utils/consistency_checker.py` | 新建一致性检查器 | 4小时 |
| P2 | `src/data/io_system.py` | 实现物品移动自动同步 | 3小时 |

### 4.2 测试清单

| 测试项 | 说明 |
|--------|------|
| 回合数增加测试 | 验证每轮行动后回合数正确增加 |
| 纯对话NPC响应测试 | 验证"和NPC说话"能触发NPC响应 |
| NPC前置回合测试 | 验证队列首位为NPC时，会先执行NPC回合再处理玩家输入 |
| 一致性检查测试 | 验证location与inventory不一致时能检测并报错 |
| 物品移动同步测试 | 验证只更新location时，inventory自动同步 |
| 存档配置恢复测试 | 验证load后world_name / npc_mode / ending规则与存档时一致 |
| 变更失败一致性测试 | 验证单条变更失败时主流程可感知并执行约定策略（中止/降级） |

---

## 5. 根本问题分析（跨问题）

> 本节从所有已确认问题中提炼系统性根因，而非针对单点bug的归因。
> 核心结论：系统模块分层已较完整，但**跨模块契约、领域不变量、失败语义**三类机制未工程化为硬约束，导致行为在边界处漂移。

### 5.1 LLM输出与代码执行之间的契约边界模糊

**涉及问题**: 0.3/0.4/0.6/1.1/1.2

**问题本质**:
系统采用"LLM决策 + 代码执行"的混合架构，但两者之间的**语义契约**未明确定义和强制校验：

| 契约缺陷 | 具体表现 | 后果 |
|---------|---------|------|
| **字段语义模糊** | `resolved`字段由LLM输出，但代码依赖其决定回合推进；`is_dialogue`与`npc_response_needed`组合控制NPC响应流程 | 流程控制受LLM输出波动影响 |
| **类型契约缺失** | LLM可能返回list而非string、返回嵌套列表污染实体集合 | 运行时类型错误、数据污染 |
| **状态机未形式化** | 回合推进、NPC响应触发等关键流程依赖多字段组合判断，无统一状态机定义 | 控制流存在早退分叉，难以测试 |

**根因诊断**:
- 关键流程控制字段（`resolved`/`is_dialogue`/`npc_response_needed`）的语义分散在Prompt约定与代码分支中
- 缺少"契约层"统一声明：字段取值范围、合法组合、驱动规则
- LLM输出校验停留在"字段存在性"，未覆盖"字段语义正确性"

### 5.2 领域不变量未下沉为数据层强约束

**涉及问题**: 0.4/1.4/1.5/1.7

**问题本质**:
系统设计为"字段写入引擎"而非"领域操作引擎"，核心业务不变量依赖调用方自觉维护：

| 不变量类型 | 当前状态 | 期望状态 |
|-----------|---------|---------|
| **位置唯一性** | 物品可同时出现在多个位置，无校验 | 物品只能处于唯一位置 |
| **持有关系双向一致** | `item.location`与`character.inventory`独立维护 | 任一变更自动同步另一端 |
| **数值边界** | HP可超过max_hp，SAN可低于0 | 自动clamp到合法范围 |
| **实体集合完整性** | ADD操作可能引入嵌套列表 | 强制扁平化、去重、类型断言 |

**根因诊断**:
- `IOSystem.apply_changes()`只验证单条变更的"语法合法性"（字段路径是否存在）
- 未验证"语义合法性"（变更后整体状态是否满足不变量）
- 领域操作（如物品转移）未封装为原子原语，需调用方手动展开为多条变更

### 5.3 变更执行缺少事务语义，失败路径不可控

**涉及问题**: 0.5/1.7

**问题本质**:
状态变更采用"尽力而为"（best-effort）模式，无事务边界、无失败聚合、无回滚机制：

```
当前行为: [变更1成功] → [变更2失败(仅日志)] → [变更3成功] → 继续流程
期望行为: [变更1成功] → [变更2失败] → 回滚变更1 → 返回失败 → 中止/降级
```

**具体表现**:
- `_apply_changes()`逐条执行，失败仅记录warning，不阻断流程
- 变更结果不聚合返回，主流程无法感知"部分成功"
- 叙事已生成但状态未落地，形成"叙事-状态分叉"

**根因诊断**:
- 变更执行层职责边界不清：只负责"尝试执行"，不负责"结果承诺"
- 主流程缺少失败策略协议：中止？降级？重试？补偿？
- 异常路径可观测性不足：静默继续导致问题难以追踪

### 5.4 运行态配置与持久化快照割裂

**涉及问题**: 1.6

**问题本质**:
引擎行为依赖两类配置：**游戏状态**（GameState）与**世界参数**（world_name/end_condition/npc_mode等），但存档只持久化前者：

| 配置类型 | 存档覆盖 | 读档恢复 | 后果 |
|---------|---------|---------|------|
| GameState | ✅ | ✅ | 正常 |
| world_name | ❌ | ❌ | 读档后可能切换到错误世界 |
| end_condition | ❌ | ❌ | 结局判定规则漂移 |
| npc_response_mode | ❌ | ❌ | NPC行为策略不一致 |
| LLM开关 | ❌ | ❌ | 叙事合并策略突变 |

**根因诊断**:
- 存档模型与运行态依赖不匹配：存的是"数据"，丢的是"行为配置"
- 缺少"最小可复现运行快照"定义：哪些字段决定行为、持久化顺序、重建步骤
- `load_game()`后未调用`apply_world_settings()`对齐运行态

### 5.5 异常处理机制缺失，错误传播不可控

**涉及问题**: 0.1/0.2/0.3/0.5

**问题本质**:
系统在多处采用"打印错误+继续执行"模式，缺少熔断、降级、恢复机制：

| 异常场景 | 当前行为 | 风险 |
|---------|---------|------|
| CLI渲染异常 | 打印错误，进入下一轮循环 | 异常刷屏 |
| 编码异常 | 崩溃 | 进程终止 |
| 类型异常 | 打印错误，继续 | 数据污染累积 |
| 变更执行异常 | 记录日志，继续 | 叙事-状态分叉 |

**根因诊断**:
- 异常处理策略不统一：有的崩溃、有的静默、有的刷屏
- 缺少"错误边界"定义：哪些错误可恢复、哪些必须熔断
- 无降级策略：异常后如何优雅降级而非硬崩溃

### 5.6 测试覆盖偏主路径，边界条件验证不足

**涉及问题**: 全部问题

**问题本质**:
回归测试主要覆盖"功能点"，未系统化覆盖"契约边界"和"异常路径"：

| 测试类型 | 当前覆盖 | 本次暴露的缺口 |
|---------|---------|---------------|
| 功能测试 | ✅ 较完整 | - |
| 契约测试 | ❌ 缺失 | 字段语义、字段组合规则 |
| 不变量测试 | ❌ 缺失 | 多变更后全局一致性 |
| 异常路径测试 | ❌ 缺失 | 部分失败、配置丢失、类型漂移 |
| 跨模块集成测试 | ⚠️ 部分 | 模块交界处的状态传递 |

**根因诊断**:
- 测试策略以"功能是否实现"为导向，未以"契约是否遵守"为导向
- 缺少不变量断言：每次变更后校验全局一致性
- LLM输出模拟不足：未覆盖异常形态（类型错误、字段缺失、语义冲突）

---

### 5.7 根因归纳与治理优先级

| 根因类别 | 核心问题 | 治理优先级 | 治理方向 |
|---------|---------|-----------|---------|
| **契约模糊** | LLM输出与代码执行语义边界不清 | P0 | 建立契约层：字段语义声明 + 状态机定义 + 输出校验 |
| **不变量缺失** | 领域约束依赖调用方自觉 | P0 | 下沉不变量：数据层强校验 + 领域操作原语 |
| **事务缺失** | 变更失败静默继续 | P1 | 引入事务语义：失败聚合 + 回滚/降级策略 |
| **配置割裂** | 存档不完整导致行为漂移 | P1 | 统一快照契约：持久化范围 + 重建步骤 |
| **异常处理** | 错误传播不可控 | P2 | 定义错误边界：熔断策略 + 降级方案 |
| **测试缺口** | 边界条件验证不足 | P2 | 补充三类测试：契约/不变量/异常路径 |

> **核心结论**: 当前问题并非单点实现失误，而是**契约未硬化 + 不变量未中心化 + 失败未事务化**的组合结果。修复时应优先做机制性改造，避免在每个症状点继续打补丁。

---

## 6. 改进计划

> 目标不是逐个修补表象，而是把“契约、状态、不变量、失败语义”四个基础层先立住，再向上恢复功能与体验。

### 6.1 阶段一：先收紧契约，冻结高风险流程

**目标**:
- 消除回合推进、NPC响应、鉴定结果显示等关键流程的语义歧义
- 让 `resolved`、`is_dialogue`、`npc_response_needed` 这类字段只承担一种明确职责

**执行项**:
1. 明确 `state_evolution_prompt.md` 中 `resolved` 的取值规则
2. 明确 `system_prompt.md` 中 `is_dialogue` 与 `npc_response_needed` 的组合规则
3. 在 `GameEngine` 内把关键流程判断收拢到统一分支，避免多处早退
4. 为关键字段增加单元测试和契约测试

**验收标准**:
- 纯对话场景可稳定触发或不触发 NPC 响应，行为由字段组合决定且可测试
- 每轮行动后的回合推进行为稳定，不再依赖隐式默认值
- 关键提示词与代码逻辑一致，不再出现“提示词说一种、代码做另一种”

### 6.2 阶段二：下沉领域不变量，封装核心领域操作

**目标**:
- 把物品、角色、地图之间的核心一致性从“调用方约定”变成“系统强约束”
- 让物品转移、持有关系同步、数值边界收敛成为统一原语

**执行项**:
1. 实现 `ConsistencyChecker`，集中检查 `location` / `inventory` / 唯一性
2. 为物品移动增加自动同步逻辑，禁止只改单边字段
3. 为 HP、SAN、实体列表等补充范围与结构化约束
4. 将“物品转移”收敛为领域原语，避免上层手写多条变更

**验收标准**:
- 任意一组变更提交后，系统能够发现并拒绝不一致状态
- 只改 `location` 时，持有关系可以自动同步或被显式拒绝
- 嵌套列表、重复实体、非法数值能在进入持久层前被拦截

### 6.3 阶段三：建立失败语义，补齐事务边界

**目标**:
- 让失败变成主流程可感知的结果，而不是静默日志
- 避免“叙事已发生、状态没落地”的分叉

**执行项**:
1. 改造 `_apply_changes()`，返回失败集合而不是只写日志
2. 在 `process_input()` 中统一决定中止、降级或重试
3. 为同一回合变更引入全成功提交策略，至少保证内存态原子性
4. 将字段可达性校验前移到 `validate_changes()`

**验收标准**:
- 单条变更失败时，主流程能明确感知并执行约定策略
- 回合叙事与状态更新不会长期分叉
- 测试中可稳定复现“失败后中止/降级”行为

### 6.4 阶段四：统一存档快照，修复运行态漂移

**目标**:
- 让存档成为可复现运行态，而不是只保存局部状态
- 保证读档后世界配置、规则、提示词策略与存档时一致

**执行项**:
1. 存档增加 world-level metadata
2. 读档时先恢复 metadata，再调用 `apply_world_settings()`
3. 增加跨世界、跨配置的读档回归测试
4. 记录存档 schema version，预留向后兼容空间

**验收标准**:
- 读档后 `world_name`、结局规则、NPC响应模式与存档时一致
- 配置切换后不会回退到默认值
- 老存档能被识别并安全迁移或降级处理

### 6.5 阶段五：补齐异常处理与测试护栏

**目标**:
- 把异常控制从“临时容错”提升为“稳定护栏”
- 让边界条件、异常路径和集成契约有持续回归能力

**执行项**:
1. 定义异常边界：哪些错误要熔断，哪些可以降级
2. CLI 保持防御性输出，避免刷屏和编码崩溃反复出现
3. 补充契约测试、不变量测试、异常路径测试
4. 对 LLM 输出增加异常形态样本测试，包括类型漂移和字段冲突

**验收标准**:
- 异常不会无限刷屏，也不会悄悄污染后续流程
- 测试能覆盖已知问题的主路径和边界路径
- 新增功能必须同时补充对应契约测试

### 6.6 推荐推进顺序

1. 先做阶段一，锁定流程契约，避免继续扩大语义漂移
2. 再做阶段二，保证数据不变量，防止修复后继续产生脏数据
3. 接着做阶段三，确保失败可感知、可中止、可回滚
4. 然后做阶段四，避免读档和跨世界配置把前面修复全部冲掉
5. 最后做阶段五，把这些规则固化成测试与异常护栏

> 执行原则: 先机制，后症状；先约束，后扩展；先回归，后优化。

---

## 附录：相关文件

### 核心文件
- `src/engine/game_engine.py` - 游戏引擎主流程
- `src/agent/state_evolution.py` - 状态推演系统
- `src/agent/dm_agent.py` - DM Agent
- `src/data/io_system.py` - IO系统
- `src/npc/npc_director.py` - NPC导演

### Prompt文件
- `src/agent/prompt/system_prompt.md` - DM Agent提示词
- `src/agent/prompt/state_evolution_prompt.md` - 状态推演提示词
- `src/agent/npc/prompt/npc_director_prompt.md` - NPC导演提示词

### 数据模型
- `src/data/models.py` - 数据模型定义
- `src/data/npc_planning_models.py` - NPC规划模型

---

> 审查完成时间: 2026-03-22  
> 下次审查建议: 修复P0级问题后进行回归测试


## 本次游玩问题归档

### 可直接修的项
1. DM 检定属性仍会混入技能词，例如 `侦查`、`图书馆使用`，会触发 `check_attributes` 校验警告。
2. `description.public` 这类嵌套字段的输出格式还不够稳定，提示词里需要继续收紧结构约束。
3. CLI/终端展示里有少量占位文本或显示不完整的问题，属于展示层细节，优先级较低，但可以顺手修。

### 机制性问题
1. 回滚现在依赖快照恢复，但如果嵌套字段已经被污染成不符合模型的形状，恢复会失败，说明事务边界还不够 schema-safe。
2. LLM 仍然会输出规则系统不支持的字段类型，单靠提示词不够，需要提示词、校验器和归一化三层一起兑底。
3. 世界配置一旦发生结构潜移，就会把问题带到运行时回合，建议把配置校验和加载烟雾测试固定下来。
4. 目前世界虽然能玩，但事件链还偏短，后续还需要继续增加线索密度和分支后果。

### 已采取的小修正
1. 已补强 DM prompt，对 `check_attributes` 的允许范围做了更明确的收束。
2. 已把失败回滚边界收紧到引擎层，并增加了失败回滚回归测试。

### 本轮归档结论
1. `check_attributes` 误混技能词的问题已从提示词与校验器两侧同时收束，运行期会被重试与校验兜住。
2. `description.public` 的结构漂移已通过数据模型归一化、IO 写入约束、回滚恢复清洗三层修复。
3. `environment.hazard` 这类不存在的字段路径已在状态推演层前置拦截，不再继续下传到执行层。
4. 事务回滚已补齐 schema-safe 恢复，脏快照不会再直接把恢复流程打爆。
5. 已补充回归测试覆盖上述场景，并通过 `python -m unittest tests.test_regression_flow` 与 `python -m unittest discover tests`。
6. 当前可将这轮错误链路视为已归档；后续若再次出现，优先检查新的 prompt 变更或世界配置污染，而不是沿用旧故障假设。
